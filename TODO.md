Upload all export posts to a different Facebook group directory.

Let pagefind search in only this directory 

tag by user account

Tag by date month year

Auto create draft listing

export should add tags 

remove title from FB exports 

a local llm to auto tag from several tags for example there may be a post mentioning some concept say about gun. 
job of LLM would be to categorize it from some given category list ( gun, EVM, social. Media etc)