{% set otherLabel = "हिंदी" if lang == "en" else "English" %}
{% set targetUrl = page.url | translatedUrl(lang) %}

<nav class="lang-switcher">
  <a href="{{ targetUrl }}">{{ otherLabel }}</a>
</nav>

